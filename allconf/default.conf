server {
    listen 80 default_server;
    server_name _;

    # 非 HTTP 方法直接丢弃
    if ($request_method !~ ^(GET|POST|HEAD|OPTIONS)$) {
        return 444;
    }

    # HTTP 但无 Host / 非法 Host
    if ($host = "") {
        return 444;
    }

    return 444;
}
